<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Dashboard</title>
</head>
<body class="bg-gray-50">
    <nav class="bg-white shadow-sm p-4 flex justify-between items-center px-10">
        <span class="font-bold text-xl text-indigo-600">AuthSystem</span>
        <a href="/logout" class="text-red-500 hover:text-red-700 font-medium">Logout</a>
    </nav>
    <main class="max-w-4xl mx-auto mt-10 p-6 bg-white rounded-xl shadow-sm">
        <h2 class="text-3xl font-bold text-gray-800">Hello, <%= user.displayName || user.username %>!</h2>
        <p class="text-gray-500 mt-2">You are successfully authenticated using <strong><%= user.provider %></strong>.</p>
        
        <div class="mt-8 p-4 bg-gray-100 rounded-lg">
            <h3 class="text-sm font-semibold text-gray-400 uppercase">Profile Data (JSON)</h3>
            <pre class="text-xs overflow-auto mt-2 text-gray-700"><%= JSON.stringify(user, null, 2) %></pre>
        </div>
    </main>
</body>
</html>