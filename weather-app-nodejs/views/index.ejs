<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather OS | Futuristic Interface</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            background: radial-gradient(circle at top left, #1e293b, #0f172a, #020617);
            font-family: 'Inter', sans-serif;
        }
        .futuristic-font { font-family: 'Orbitron', sans-serif; }
        .glass-panel {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.8);
        }
        .neon-glow {
            text-shadow: 0 0 10px rgba(56, 189, 248, 0.8);
        }
        .neon-border:focus {
            border-color: #38bdf8;
            box-shadow: 0 0 15px rgba(56, 189, 248, 0.4);
        }
    </style>
</head>
<body class="text-slate-200 flex items-center justify-center min-h-screen p-4">

    <div class="absolute top-10 left-10 w-64 h-64 bg-cyan-500/10 rounded-full blur-[100px] animate-pulse"></div>
    <div class="absolute bottom-10 right-10 w-80 h-80 bg-blue-600/10 rounded-full blur-[120px]"></div>

    <div class="glass-panel p-8 rounded-3xl w-full max-w-md relative z-10 overflow-hidden">
        <div class="mb-8">
            <h1 class="futuristic-font text-3xl font-bold tracking-widest text-cyan-400 neon-glow uppercase">
                Weather<span class="text-white">OS</span>
            </h1>
            <p class="text-xs text-slate-400 tracking-tighter uppercase mt-1">Satellite Uplink Active</p>
        </div>
        
        <form action="/weather" method="POST" class="space-y-4">
            <div class="relative">
                <input type="text" name="city" placeholder="SEARCH COORDINATES..." required 
                    class="bg-slate-900/50 border border-slate-700 p-4 rounded-xl w-full outline-none neon-border transition-all futuristic-font text-sm tracking-widest placeholder:text-slate-600">
            </div>
            <button type="submit" 
                class="bg-cyan-500 hover:bg-cyan-400 text-slate-950 font-bold py-3 rounded-xl w-full transition-all transform hover:scale-[1.02] active:scale-95 futuristic-font tracking-widest text-sm shadow-[0_0_20px_rgba(6,182,212,0.5)]">
                EXECUTE SCAN
            </button>
        </form>

        <% if(weather !== null) { %>
            <div class="mt-10 animate-in fade-in slide-in-from-bottom-4 duration-700">
                <div class="flex flex-col items-center">
                    <h2 class="text-2xl font-light tracking-widest mb-2"><%= weather.city %></h2>
                    
                    <div class="relative">
                        <div class="absolute inset-0 bg-cyan-400/20 blur-3xl rounded-full"></div>
                        <img class="relative w-32 h-32" src="http://openweathermap.org/img/wn/<%= weather.icon %>@4x.png" alt="icon">
                    </div>

                    <p class="text-7xl font-bold futuristic-font mt-2 text-white">
                        <%= weather.temp %><span class="text-cyan-400 text-4xl">°</span>
                    </p>
                    
                    <div class="mt-4 px-4 py-1 bg-cyan-500/10 border border-cyan-500/20 rounded-full">
                        <p class="capitalize text-cyan-400 text-xs font-semibold tracking-[0.2em]"><%= weather.description %></p>
                    </div>

                    <div class="grid grid-cols-2 gap-4 w-full mt-8 border-t border-slate-700/50 pt-6">
                        <div class="text-center">
                            <p class="text-[10px] text-slate-500 uppercase tracking-widest">Visibility</p>
                            <p class="text-sm font-semibold">Maximum</p>
                        </div>
                        <div class="text-center">
                            <p class="text-[10px] text-slate-500 uppercase tracking-widest">System Status</p>
                            <p class="text-sm font-semibold text-emerald-400">Stable</p>
                        </div>
                    </div>
                </div>
            </div>
        <% } %>

        <% if(error !== null) { %>
            <div class="mt-6 p-3 bg-red-500/10 border border-red-500/50 rounded-lg">
                <p class="text-red-400 text-xs futuristic-font text-center uppercase tracking-tighter"><%= error %></p>
            </div>
        <% } %>
    </div>

    <div class="absolute bottom-4 text-[10px] text-slate-600 tracking-[0.3em] uppercase">
        Developed by <span class="text-slate-400">saadxsalman</span> // Terminal v4.0
    </div>
</body>
</html>